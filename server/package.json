{
  "name": "river-clearance-sync-api",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal username-keyed sync backend for the River Bridge Clearance app's vessel list. No passwords -- placeholder until real hosting + real auth exists.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0"
  }
}
