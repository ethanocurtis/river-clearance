name: Build (placeholder)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show tree
        run: |
          ls -R
